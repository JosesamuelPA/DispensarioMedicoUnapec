@model IEnumerable<DispensarioMedicoUnapec.Models.Paciente>

@{
    ViewData["Title"] = "Index";
}

<div class="row mb-4 align-items-center">
    <div class="col-md-8">
        <h1 class="h3 fw-bold mb-0" style="color:#0d7ff2;">
            <i class="bi bi-people-fill me-2"></i>Gestión de Pacientes
        </h1>
        <p class="text-muted mt-1 mb-0">Directorio y control de pacientes registrados en el sistema</p>
    </div>
    <div class="col-md-4 text-md-end mt-2 mt-md-0">
        <a asp-action="Create" class="btn btn-primary rounded-pill px-4 shadow-sm">
            <i class="bi bi-plus-circle me-2"></i>Nuevo Paciente
        </a>
    </div>
</div>

<div class="card mb-4">
    <div class="card-body">
        <form id="filterForm">
            <div class="row g-3">
                <div class="col-md-2">
                    <input name="filter_name" class="form-control" placeholder="Nombre" />
                </div>
                <div class="col-md-2">
                    <input name="filter_lastName" class="form-control" placeholder="Apellido" />
                </div>
                <div class="col-md-2">
                    <input name="filter_id" class="form-control" placeholder="Cedula" />
                </div>
                <div class="col-md-2">
                    <select name="filter_typePatients" class="form-select">
                        <option>Todos</option>
                        @foreach (var tipoPacientes in Enum.GetValues<TipoPaciente>())
                        {
                            <option>@tipoPacientes</option>
                        }
                    </select>
                </div>
                <div class="col-md-2">
                    <select id="filter_statusPatients" name="filter_statusPatients" class="form-select">
                        <option>Todos</option>
                        @foreach (var estadoPaciente in Enum.GetValues<EstadoPaciente>())
                        {
                            <option>@estadoPaciente</option>
                        }
                    </select>
                </div>
                <div class="col-md-2 d-flex gap-2">
                    <button type="submit" class="btn btn-success w-100" title="Buscar"><i class="bi bi-search"></i></button>
                    <button type="button" id="btnLimpiar" class="btn btn-secondary w-100" title="Limpiar"><i class="bi bi-eraser"></i></button>
                </div>
            </div>
        </form>
    </div>
</div>

<div id="table-container">
    <partial name="_PacientesTable" model="Model" />
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#filterForm').on('submit', function (e) {
                e.preventDefault();
                $.ajax({
                    url: '@Url.Action("Filter", "Pacientes")',
                    type: 'GET',
                    data: $(this).serialize(),
                    success: function (result) {
                        $('#table-container').html(result);
                    },
                    error: function (xhr, status, error) {
                        console.error("Error al filtrar: ", error);
                    }
                });
            });

            $('#btnLimpiar').on('click', function () {
                $('#filterForm')[0].reset();
                $('#filterForm').submit();
            });
        });

        function loadPage(page) {
            var data = $('#filterForm').serialize() + '&page=' + page;
            $.ajax({
                url: '@Url.Action("Filter", "Pacientes")',
                type: 'GET',
                data: data,
                success: function (result) {
                    $('#table-container').html(result);
                },
                error: function (xhr, status, error) {
                    console.error("Error en paginación: ", error);
                }
            });
        }
    </script>
}
